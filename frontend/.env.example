# Same-origin API by default (when frontend is served from the same Vercel project as API)
VITE_API_BASE_URL=

# Supabase Auth (browser-safe values)
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxx
